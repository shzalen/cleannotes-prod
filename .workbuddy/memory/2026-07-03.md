# 2026-07-03

## 工作空间迁移
- 项目从 `D:\WorkBuddySpace\ZURU\2026-06-10-10-28-51\cleannotes\` 迁移至 `D:\CleanNotepad\v1.3.0\`
- 更新 MEMORY.md：修正项目路径、Git 状态、默认修改主项目约定
- 删除了 MEMORY.md 中重复的备忘录模块描述段落
- 新 repo Git commit: c500f44（旧 repo: d4aae18, tag v1.0-stable）
- 清理旧工作空间路径引用：移除 all-in-one 适配副本、移除旧 Git 状态引用、简化"默认修改主项目"约定
- 完善 README.md，覆盖技术栈、功能模块、项目结构、快速开始、关键约定
- 更新 MEMORY.md Git 状态为已配置 remote https://github.com/shzalen/cleannotes.git
- Commit 26aff3c: README, Commit 8ae6d81: MEMORY, Push 失败（GitHub HTTPS 认证未通过）

## 周报界面科技化重构
- ReportsView.vue 全面视觉重构，参考腾讯系产品设计语言
- 主要改动：
  - 全局点阵网格背景（grid-overlay），科技仪表盘感
  - Hero 区增加渐变 mesh 背景 + 内部点阵，取代旧纯白 hero
  - 侧栏 header 增加渐变装饰线（header-bar）
  - 生成按钮改为渐变背景 + shimmer 动画扫光
  - 周报卡片增加 hover 发光边框 + accent bar 辉光扩散
  - 进度条增加 shimmer 扫光动画
  - 完成率圆环增加外层脉冲辉光（ring-glow）
  - 统计标签改为大写英文（COMPLETION/DONE/NEW/XP），增加 tabular-nums
  - Stat 卡片 hover 增加发光边框 + 上浮效果
  - Section 编号改为带背景色和边框的标签样式
  - Section 分隔线改为渐变消隐
  - Footer 渐变装饰线取代旧实线
  - 所有动画：shimmer/pulse-ring/glow-border/fadeIn
- 全部样式遵守 CSS 变量公约（.vue <style> 无 hex），兼容 light/dark/zuru 三主题

## 周报海报模式
- 新增 `src/components/ReportPoster.vue` — 全屏海报展示组件
- 海报布局（440px 固定宽度，纵向）：
  - Hero 区：品牌标识 + 年份 + 大号 W{周数} + 日期范围 + 130px 完成率圆环
  - 统计行：完成任务/新增任务/经验获得/连续天数（4 项横排）
  - 01 本周完成任务：列表（优先级色点 + 名称 + 优先级标签 + 实际开始时间 + 耗时 + 实际完成日期），最多 10 条
  - 02 下周跟进：待完成任务列表（优先级色点 + 名称 + 截止日期），最多 8 条
  - Footer：渐变装饰线 + 品牌 logo
- 背景装饰：点阵网格 + 双色辉光 blob（primary/accent 模糊圆）
- 入口：ReportsView hero 区右侧新增「海报」按钮，点击打开全屏 overlay
- 导出功能：html2canvas-pro 动态 import，2x scale 导出 PNG，文件名含周数+日期
- html2canvas-pro 作为独立 chunk（~246KB），仅导出时加载，不影响首屏
- ESC 键关闭海报，点击遮罩关闭
- 兼容 light/dark/zuru 三主题（全部使用 CSS 变量）

## 周报海报导出修复 + 布局重构
- **海报导出修复**：html2canvas-pro 无法解析 `var(--color-*)` 和 `color-mix()`，导致导出 PNG 背景透明、内容缺失
  - 修复方案：`onclone` 回调中预收集所有元素的 `getComputedStyle` 值，内联到克隆 DOM；同时在克隆文档注入 `:root` CSS 变量解析值
- 海报宽度 440px → 480px → 540px（增加开始时间+耗时列后需更多空间）
- 涉及属性：backgroundColor/color/borderColor/backgroundImage/boxShadow/opacity/fill/stroke 等

## 周报任务清单字段扩展
- weeklyReport.ts generateReportContent：完成日期列改为「实际完成日期」，新增「实际开始时间」（inProgressAt → YYYY-MM-DD HH:MM 本地时间）和「耗时」（formatDuration）两列
- 列顺序：任务名称 | 优先级 | 实际开始时间 | 耗时 | 实际完成日期
- ReportPoster.vue：已完成任务行新增 task-start（开始时间）和 task-duration（耗时）span，紧跟 task-pri 之后
- 新增 formatReportDateTime / formatDateTime 辅助函数（UTC ISO → 本地 YYYY-MM-DD HH:MM）
- 导入 formatDuration 从 task store
- 耗时列宽度调整：HTML 表格 80px→100px + white-space:nowrap；海报 task-duration min-width 40px→56px + white-space:nowrap；task-start/task-date min-width 68px→80px

## 周报 AI 智能总结
- WeeklyReport 类型增加 `aiSummary?: string` 和 `aiSummaryStatus?: 'generating' | 'success' | 'failed'` 字段
- weeklyReport.ts 两阶段生成：
  - Phase 1 `generateReport()`：同步返回报告（AI 占位为 generating），立即保存和显示
  - Phase 2 `generateAiSummary()`：异步调用 AI，完成后更新报告 HTML 和状态（success/failed）
- 三种 AI 状态占位 HTML：
  - generating：shimmer 扫光条 + "AI 总结正在生成中，请稍后查看"
  - failed：✕ icon + "AI 总结暂不可用"
  - success：实际 AI 总结段落
- ReportsView.vue：`handleGenerate` 改为 Phase 1 立即显示 + Phase 2 后台更新
- ReportPoster.vue：AI section 条件渲染三种状态（generating/failed/success）
- supabase.ts：增加 ai_summary_status TEXT 字段映射
- migration-ai-summary.sql：增加 ai_summary_status 列（CHECK IN ('generating','success','failed'))
- **ReportsView 布局重构为备忘录风格**：
  - 侧栏 320px → 300px，背景 surface → bg-2
  - 报告卡片从 elaborate card（accent bar + progress + stats grid）简化为 memo-style list item（week num + label + rate → meta line → hover delete）
  - 右侧面板从 hero section 改为 header bar（week badge + title + date + compact ring + poster btn + delete btn）
  - 内容区改为 flex column（header + scrollable content + footer），与 MemoView 结构一致
  - 移除旧 hero section（gradient mesh + grid bg + large ring + pulse animation）
  - 保留科技感元素：grid overlay、tabular-nums、渐变装饰线、shimmer 动画

## 任务创建日期默认值修复
- 修复 TaskEditModal.openNew() 中 startDate 不跟随日历选中日期的问题
- 将 `startDate.value = toLocalDate()` 改为 `startDate.value = date || toLocalDate()`
- 现在在日历中选中某个日期后点击「添加」，开始日期将默认为日历选中日期（不再永远是今天）

## 任务状态筛选功能
- 在 TaskRightPanel 新增状态筛选栏（全部/待办/进行中/已完成），使用 segment control 风格按钮组
- 筛选栏位于时间范围标签页（日/周/月/季/年）下方、任务列表上方
- 在 dayTasks/weekTasks/monthTasks/quarterTasks/yearTasks 五个 computed 的返回语句中追加状态过滤
- TasksView 通过 v-model:statusFilter 将已有的 filter ref 传给 TaskRightPanel
- TasksView 的 filteredTasks computed（中心统计卡片用）也追加了状态过滤逻辑
- 空状态文案跟随筛选状态动态变化（如"该日期无已完成任务"）
- 路由联动保留：支持 ?status=todo 等 URL 参数直接激活筛选
