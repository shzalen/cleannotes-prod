# 2026-07-15 H5 移动端 UI 全面优化

## 完成内容

## 弹窗禁止缩放和水平移动（17:30）
- 用户反馈：创建和编辑弹窗界面不需要缩放的左右移动
- 修复：三个移动端弹窗组件容器添加 `touch-action: pan-y` + `overflow-x: hidden`
  - MobileTaskEditPopup.vue — `.edit-popup`
  - MobileTaskDetailPopup.vue — `.detail-popup`
  - MobileTaskProgressPopup.vue — `.progress-popup`
- `touch-action: pan-y` 只允许垂直触摸手势，禁止捏合缩放（pinch-zoom）和水平平移
- 构建：2865 modules, 23.81s；robocopy 同步到生产目录

## 修复三个 Tab 页整体滚动问题（17:32）
- 问题：首页、日历、我的三个页面变成整体滑动，而非内容区独立滚动
- 根因：`#app` 有 `height: 100dvh` 固定高度但无 `overflow: hidden`，内容溢出穿透到 body 层
- 修复：`style.css` 中 `#app` 添加 `overflow: hidden`，切断滚动穿透
- 高度链：body(100%) → #app(100dvh, overflow:hidden) → .m-layout(100%) → .m-layout__body(flex:1, overflow:hidden) → 页面内容区各自独立滚动
- 构建：2865 modules, 29.16s；robocopy 同步到生产目录

## 任务项圆点与 PC 端逻辑对齐（17:35）
- 需求：移动端任务项圆点逻辑与 PC 端 TodayProgress 一致——待办灰色、进行中橙色、到期未完成动态脉冲线圈
- PC 端参考：TodayProgress.vue 的 `.timeline-dot` + `isTimeReached` + `::after` 脉冲动画
- 修改文件：`MobileHome.vue` + `MobileCalendar.vue`
- 三处改动：
  1. **Script**：添加 `onMounted/onUnmounted`、30s 定时 `now` ref、`isTimeReached(task)` 函数（与 PC 端逻辑一致：startTime 已到且未完成 → true）
  2. **Template**：`task-item__check` 添加 `:class="[task.status, { 'is-due': isTimeReached(task) }]"`
  3. **Style**：替换原统一灰色圆点为 status 着色（todo=灰、in_progress=橙、done=绿）+ `is-due::after` 脉冲线圈动画（1.2s ease-out，从 0.6 缩放到 1.8 并淡出）
- 构建：2865 modules, 26.31s；robocopy 同步到生产目录

### 一、首页优化 (MobileHome.vue)
- 今日完成率卡片移至 header 下方、任务列表上方
- Header 区域替换为 MobileGreetingCard 组件（问候语 + 日期 + 随机语，等同 PC 端 GreetingCard.vue）
- 任务项：长按 1 秒 → MobileTaskProgressPopup（进度更新弹窗）；点击 → MobileTaskDetailPopup（详情弹窗）
- FAB 按钮 → MobileTaskEditPopup（占页面 78% 高度，完整创建/编辑表单）
- 添加 van-pull-refresh 橡皮筋效果

### 二、日历页优化 (MobileCalendar.vue)
- "+新增"按钮 → MobileTaskEditPopup 完整创建弹窗
- 任务列表项：同首页长按/点击行为
- 添加 van-pull-refresh

### 三、我的页面优化 (MobileProfile.vue)
- 主题选择：改为单行下拉选择器
- 新增：修改密码（底部弹窗表单）、清空缓存（确认对话框 + 清除 localStorage/sessionStorage + 重载）
- 新增：修改昵称（底部弹窗表单）
- 新增：待办事项 / 备忘录 / 周报 子应用入口（MobileSubApp 全屏覆盖 + Teleport）

### 四、框架优化
- 首页、日历页添加 van-pull-refresh 橡皮筋效果
- 登录页字体放大 ~20%
- style.css 修复 pull-refresh overflow 覆盖样式

## 新增文件（8 个组件）
- `src/mobile/components/MobileGreetingCard.vue`
- `src/mobile/components/MobileTaskDetailPopup.vue`
- `src/mobile/components/MobileTaskProgressPopup.vue`
- `src/mobile/components/MobileTaskEditPopup.vue`
- `src/mobile/components/MobileSubApp.vue`
- `src/mobile/components/MobileTodoApp.vue`
- `src/mobile/components/MobileMemoApp.vue`
- `src/mobile/components/MobileWeeklyApp.vue`

## 修复
- MobileWeeklyApp.vue：`reportStore.reports` → `reportStore.sortedReports`；`reportStore.getWeekLabel()` → 直接 import `getWeekLabel`
- 构建：workbuddy safe-delete 阻止 dist 清理 → 手动 rm -rf 后构建成功

## 构建结果
- 2861 modules, 35.48s, 成功
- robocopy 同步 131 文件到 D:\CleanNotepad-Prod\v1.3.0\

## Git 合并冲突 (spa → main)
- origin/main 有独立 H5 实现（TaskDetailSheet 等），与 spa 新实现完全冲突，共 17 个文件
- 因冲突文件过多（尤其 package-lock.json），GitHub Web 无法处理，需命令行 `-X ours` 解决
- 用户确认后本地执行合并

## 会话约定
- 后续默认 H5 优化会话，请求默认指向 `src/mobile/`

## 触控防误触优化 (useTouchInteraction)
- 问题：首页下拉时容易误触任务项（touch 位移极小被判定为 tap 打开详情）
- 方案一：位移阈值拦截（> 15px 视为滚动/下拉，取消一切交互）
- 新增 `src/mobile/composables/useTouchInteraction.ts`
  - 判定规则：位移 > 15px → 取消；位移 ≤ 15px + duration < 1s → tap；位移 ≤ 15px + duration ≥ 1s → long-press（带振动）
  - 泛型设计 `useTouchInteraction<T>`，返回 `{ handleTouchStart, handleTouchMove, handleTouchEnd }`
- MobileHome.vue 和 MobileCalendar.vue 任务项触控已改为使用 composable
- 构建成功：2862 modules, 28s；robocopy 同步 132 文件到生产目录

## H5 版本号显示
- MobileProfile.vue 底部改用 `__APP_VERSION__` + `__BUILD_TIME__` 展示版本号与构建时间（commit: f3e61d2）
- 样式：居中、淡色字、半透明，与 PC 端 AppSidebar 版本信息风格一致

## Vant 字体放大修复
- 问题：MobileLogin.vue 自定义 CSS 字体已放大，但 van-field、van-button 等 Vant 组件使用默认 14px/16px 字号未覆盖
- 修复：在 src/mobile/style.css Vant 桥接区追加 --van-font-size-md、--van-cell-font-size 等变量，全局放大 ~20%
- 构建成功：2862 modules, 24.4s；robocopy 同步 131 文件

## 提交公约调整
- 用户要求不再由 AI 执行 git add/commit，改由用户自行处理。仅负责改代码 + build + robocopy。

## 修复 H5 启动报错 authReady is not defined
- 问题：PC 浏览器打开 H5 报错 `ReferenceError: authReady is not defined`
- 根因：`src/mobile/router/index.ts` 第 49 行 `authReady = true` 使用了未声明变量
- 修复：删除该未使用的变量赋值，仅保留 `await auth.init()`
- 构建：2862 modules, 23.51s, 成功；robocopy 同步 131 文件到生产目录

## 创建/编辑任务弹窗改为上下结构
- 问题：MobileTaskEditPopup.vue 中标题、日期、时间、截止日期仍用 Vant `van-field`（标签左/输入右），与描述/优先级上下结构不一致
- 修改：将上述字段全部改为 `<label class="ep-label"> + <input>` 的上下结构，移除 `van-field` 和 `van-cell-group`
- 统一字体：`.ep-label`、`.ep-input`、`.ep-textarea`、`.ep-radio-btn` 全部调整为 17px，与全局 Vant 字体放大一致
- 构建：2862 modules, 36.35s, 成功；robocopy 同步 131 文件到生产目录

## 桌面图标设计
- 腾讯蓝版图标 SVG：腾讯蓝底（#0052D9）+ 三层纸张（珊瑚/浅蓝/白）+ 珊瑚笔身琥珀夹 + 紫色笔尖 + 绿/红/橙优先级圆点
- 图标以 data URI 形式嵌入 `public/manifest.json`（192x192 + 512x512）
- 同时保存源文件 `public/icon.svg` 供后续修改
- 构建：2862 modules, 25.73s, 成功；robocopy 同步 133 文件到生产目录

## 禁止双击缩放
- 问题：手机端双击页面会触发浏览器缩放
- viewport 已设 `user-scalable=no, maximum-scale=1.0`，但部分浏览器（iOS Safari 10+）会忽略
- CSS 加固：`src/mobile/style.css` body 追加 `touch-action: manipulation`
- JS 兜底：`src/mobile.ts` 中 `document` 级别拦截 `gesturestart`（iOS 捏合手势）和 `dblclick`

## 橡皮筋阻尼效果恢复
- 问题：首页/日历页橡皮筋阻尼效果不如"我的"页面自然
- 根因：van-pull-refresh 内部嵌套 overflow-y:auto 容器，两层滚动冲突，van-pull-refresh 无法感知滚动边界
- 修复：移除首页和日历页的 van-pull-refresh，改用原生 overflow-y:auto + -webkit-overflow-scrolling:touch 滚动容器
- 同时移除未使用的 showToast 导入
- 构建：2862 modules, 24.75s；robocopy 同步 131 文件到生产目录

## 登录页重设计（简约大气）
- 自建输入框/按钮替换 Vant 组件：80px logo、56px 高输入框/按钮、18-20px 字、12px 圆角
- 聚焦态腾讯蓝光晕（box-shadow），按钮字间距"登 录"
- 移除副标题，品牌区 padding 增大

## Flexbox 滚动修复
- 问题：移除 van-pull-refresh 后首页和日历页完全无法滚动
- 根因：flexbox 中 `flex: 1` 子元素默认 `min-height: auto`，即使设了 `overflow-y: auto` 也不会缩小；外层容器缺少 `overflow: hidden` 约束
- 修复：`.home-page` / `.cal-page` 加 `overflow: hidden`；`.home-content` / `.cal-content` 加 `min-height: 0`
- 构建：2862 modules, 30.27s；robocopy 同步 133 文件到生产目录

## 桌面图标调整为 85%
- 用户从 58%/70%/75%/80%/85% 五个比例中选定 85%
- SVG 通过 `<g transform="translate(48, 53) scale(0.85)">` 包裹内容元素实现居中缩放
- 同步更新 manifest.json data URI
- 构建：同上

## 「我的」页面字体调大
- 用户要求文字大小与清除缓存确认对话框中的文字一致
- Vant Dialog 消息字号 17px（--van-font-size-md）、标题 19px（--van-font-size-lg）
- 调整 `.profile-card__name` 18→19px、`.profile-menu__label` 15→17px、`.profile-card__email` 13→17px、`.profile-menu__select` 14→17px、`.profile-menu__value` 13→17px、`.profile-section__title` 13→17px、`.profile-card__edit-hint` 11→14px、`.profile-menu__hint` 11→15px、`.profile-version` 11→14px、`.edit-sheet__title` 16→19px、`.edit-sheet__error` 13→17px
- 构建：2862 modules, 32.03s；robocopy 同步 131 文件

## Flexbox 滚动修复（第二轮）
- 问题：上次加 `min-height: 0` + `overflow: hidden` 后首页和日历页仍无法滚动
- 根因：父容器 `MobileLayout.vue` 的 `.m-layout__body` 是 flex 容器（`flex: 1; display: flex; flex-direction: column`），子页面使用 `height: 100%` 无法在 flex 上下文中解析出确定高度 → 子页面内部的滚动区域高度为 0
- 修复：`.home-page` / `.cal-page` 将 `height: 100%` 改为 `flex: 1; min-height: 0`，使其作为 flex 子元素正确参与布局
- 构建：2862 modules, 26.69s；robocopy 同步 131 文件到生产目录

## 移动端 UI 一致性修复（5 项，2026-07-15 下午）
### 1. 弹窗字体统一为 17px（以任务编辑弹窗为准）
- MobileTaskDetailPopup: `.detail-field__label` 11→17px, `.detail-field__value` 13→17px, `.detail-field__desc` 13→17px, `.detail-field__tag` 12→15px
- MobileTaskProgressPopup: `.pp-label` 12→17px, `.pp-status-btn` 14→17px, `.pp-input` 13→17px, `.progress-popup__title` 15→16px
- MobileProfile edit-sheet: `.edit-sheet__title` 19→16px

### 2. 内容页字体统一为 15px（以首页任务项标题为准）
- MobileProfile: `.profile-card__name` 19→15px, `.profile-card__email` 17→15px, `.profile-menu__label` 17→15px, `.profile-menu__value` 17→15px, `.profile-menu__select` 17→15px, `.profile-section__title` 17→15px, `.profile-menu__hint` 15→13px

### 3. TabBar 底部空白修复
- `style.css`: html/body 和 #app 使用 `height: 100dvh`（fallback `100%`）+ `overflow: hidden`，解决移动端地址栏导致视口计算偏差
- `MobileLayout.vue`: `.m-layout` 也加 `height: 100dvh`

### 4. 弹窗统一单栏布局
- MobileTaskDetailPopup: `.detail-row--2col` `grid-template-columns: 1fr 1fr` → `1fr`
- MobileTaskProgressPopup: `.pp-time-grid` `grid-template-columns: 1fr 1fr` → `1fr`

### 5. 弹窗高度统一（内容少的除外）
- MobileTaskEditPopup: 78% → 75%
- MobileTaskDetailPopup: 70% → 75%
- MobileTaskProgressPopup/MobileProfile sheets: 保持内容自适应（内容少）

- 构建：2862 modules, 25.24s；robocopy 同步到生产目录

## 滚动修复回退 + 详情页两栏恢复（2026-07-15 17:20）
- 问题1：详情页上一轮误改为单栏，用户要求"左右两栏"
- 问题2：上一轮在 html,body 加 `overflow:hidden` 切断 flex 布局链，导致整页滚动而非内容区独立滚动
- 修复：
  - `MobileTaskDetailPopup.vue`：`.detail-row--2col` 恢复 `grid-template-columns: 1fr 1fr`（字体保持 17px 不变）
  - `style.css`：移除 `html,body` 的 `overflow:hidden` 和 `100dvh`；`100dvh` 仅保留在 `#app`，不可在 body 级别加 overflow:hidden
  - `MobileLayout.vue`：移除多余 `100dvh`，仅用 `height:100%`（从 `#app` 继承 `100dvh`）
- 正确 Flex 链：`body:100%` → `#app:100dvh` → `.m-layout:100%` → `.m-layout__body:flex:1;overflow:hidden` → `.home-page/.cal-page:flex:1;min-height:0` → `.home-content/.cal-content:flex:1;overflow-y:auto`
- 底部空白通过 `#app` 的 `100dvh` 解决，不破坏内容区独立滚动
- 构建：2862 modules, 30.80s；robocopy 同步到生产目录

## 首页天气组件（2026-07-15 17:26）
- 新增 `MobileWeatherWidget.vue`，显示在问候语卡片右侧
- 天气数据源：Open-Meteo 免费 API（无需 key），通过浏览器 Geolocation 定位
- 天气码映射 WMO 标准（0 晴 / 1-3 多云 / 45-48 雾 / 51-65 雨 / 71-86 雪 / 95-99 雷暴）
- 动态 SVG 动画：
  - 下雨：5 条雨滴同时下落循环动画
  - 下雪：6 颗雪花飘落+旋转动画
  - 雷暴：闪电闪烁动画
  - 晴天：太阳旋转动画
  - 多云/雾：静态图标
- 温度 13px 粗体白色 + 天气描述 9px 半透明白色
- 异常降级：定位失败/超时 → 半透明文字提示
- 集成位置：`MobileGreetingCard.vue` `.m-greeting__row`（flex justify-between），左问候语右天气
- 构建：2865 modules, 22.12s；robocopy 同步到生产目录