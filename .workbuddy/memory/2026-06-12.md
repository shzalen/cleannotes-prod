# 2026-06-12 工作日志

- GreetingCard.vue 日期行增加 ISO 周数显示：`2026年6月12日 周五 · 第24周 15:33:58`，新增 `getISOWeekNumber` 函数按 ISO 8601 标准计算
- TaskEditModal.vue Markdown 增强：
  - 修复回车丢失：`marked.parse()` 增加 `{ breaks: true }` 选项，单换行也能渲染为 `<br>`
  - TaskRightPanel.vue 的 `detailHtml` 同步修复
  - 新增 Markdown 格式化工具栏：10 个快捷按钮（粗体/斜体/标题/无序列表/有序列表/任务列表/行内代码/代码块/链接/引用），支持选中文本包裹和光标处插入
  - 改善预览区排版：字号 14px、行高 1.75、PingFang SC 字体栈、h1 底部分割线、blockquote 左边框改 primary 色加背景、strong 加粗着色、链接下划线、task list checkbox 样式
  - 编辑器 textarea 圆角改为仅下方圆角（工具栏占上方）
- 用户确认使用方式：dist 挂 IIS，修改后必须 build 才能生效
- TaskEditModal.vue Markdown 编辑器全屏模式：工具栏右侧增加全屏图标按钮，点击后整个编辑器容器（工具栏+textarea）fixed 全屏展示，textarea 自适应占满剩余空间、最大宽度 960px 居中；Esc 优先退出全屏而非关闭弹窗；关闭弹窗时自动重置全屏状态
- TaskEditModal.vue Tab 缩进：textarea 拦截 Tab 键插入 2 空格，Shift+Tab 删除行首最多 2 空格反缩进；支持多行选中整体缩进/反缩进
- TaskEditModal.vue Markdown 编辑器三项优化：
  1. 编辑框自适应：desc-group flex:1 填满 modal-body 剩余空间，textarea 自动撑满高度，取消 max-height 限制
  2. 多行选中缩进：选中多行后 Tab/Shift+Tab 对每行统一加/减 2 空格
  3. Ctrl+Z 撤销：所有程序化文本插入（工具栏按钮、Tab缩进）改用 document.execCommand('insertText')，使修改进入浏览器原生撤销栈

## 任务 startDate/startTime 排序重构 [2026-06-12]

### 变更内容
1. **Task 类型新增 `startDate: string | null` 字段**（`src/types/index.ts`）
2. **store task.ts**：`addTask`/`updateTask` 支持 `startDate`
3. **supabase.ts**：四组转换函数新增 `start_date` ↔ `startDate` 映射
4. **首页今日任务列表（TodayProgress.vue）**：
   - 过滤逻辑改为优先按 `startDate` 判定，无 `startDate` 的旧数据回退到 `createdAt`
   - 排序逻辑改为：未完成按 `startDate+startTime` 升序；已完成按 `completedAt` 降序排在后面
5. **HomeView.vue**：`isTodayTask` 逻辑与 TodayProgress 对齐
6. **TaskRightPanel.vue**：
   - 日/周/月/季/年 视图过滤逻辑均改为优先按 `startDate`
   - `sortAligned` 排序函数同步更新
   - 日期标签显示「计划 MM-DD」或「创建 MM-DD」
7. **TasksView.vue**：`filteredTasks` 过滤逻辑对齐
8. **TaskEditModal.vue**：表单新增「开始日期」字段（date 类型），与「时间节点」并列

### Supabase 待执行 SQL
```sql
ALTER TABLE cleannote_tasks ADD COLUMN IF NOT EXISTS start_date TEXT;
ALTER TABLE cleannote_deleted_tasks ADD COLUMN IF NOT EXISTS start_date TEXT;
```

### 旧数据兼容
- 所有过滤/排序逻辑对 `startDate === null` 的任务均回退到 `createdAt` 逻辑
- 已有任务不受影响，新增任务可选填开始日期

### 构建状态
- `npx vite build` 成功，无编译错误
