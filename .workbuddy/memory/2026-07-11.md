# 2026-07-11

## 全面安全与性能审计（第二轮）

对整个系统执行了 5 维度并行审计（认证授权 / 数据存储传输 / 前端安全 / 构建加载 / 运行时网络），发现 37 项问题 + 18 项已通过检查。

### 严重 (3 项)
- **S/P-01 跨标签页同步缺失**：todo.ts / weeklyReport.ts / growth.ts 从不调用 broadcastChange()，跨标签页数据不同步
- **P-01 memo 搜索 DOMParser**：stripHtml() 对每个 memo 调用 DOMParser，搜索 100 条 = 100 次解析，极卡
- **P-02 dragover 监听器泄漏**：RichTextEditor add/removeEventListener 传入不同函数引用，监听器永不移除

### 高 (8 项)
- S-02 CSP 'unsafe-eval' 削弱 XSS 防护
- S-03 Link URL 验证仅阻止 javascript: 前缀，可被 data:/vbscript:/空格等绕过
- S-04 登出 fire-and-forget，缓存 JWT 未及时清除
- S-05 AI API Key 明文存储在数据库
- S-06 旧 RLS 策略可能仍使用 x-user-id header
- S-07 Supabase anon key 硬编码 + 项目未使用环境变量
- S-08 DiagView 暴露全部 localStorage 含 JWT
- P-03 @supabase/supabase-js (8.2MB) 被拉入入口 chunk

### 关键修复建议
1. todo/weeklyReport/growth 添加 broadcastChange() — 改动量小收益高
2. stripHtml 改用正则 replace 替代 DOMParser
3. dragover 监听器提取具名函数引用
4. Link validate 改为 URL 白名单（http/https/mailto/tel）
5. logout() 先 setCachedAccessToken(null) 再后台 signOut
6. vite.config.ts 添加 vendor-supabase manualChunk

### 报告位置
`public/test-reports/v1.3.0-security-perf-audit.html`

## 第二轮安全与性能审计（复查）

在第一轮全部 37 项修复完成后，对系统做了全面复查。结果：**0 严重 / 0 高 / 2 中 / 9 低 / 42 已通过**。

### 新发现问题 (11 项)
- **R2-S01 (中)** supabaseClient.ts 仍保留 anon key 硬编码 fallback
- **R2-S02 (中)** AES-GCM 加密 APP_SECRET 硬编码在客户端（架构限制，可接受）
- **R2-S03 (低)** 文件下载 a.target='_blank' 缺少 rel="noopener noreferrer"
- **R2-S04 (低)** hybrid.ts 注释仍引用 x-user-id header（过时文档）
- **R2-S05 (低)** SlashCommand innerHTML 使用预定义字符串（防御性建议）
- **R2-S06 (低)** Storage API 错误消息未脱敏（S-13 修复的遗漏）
- **R2-P01 (低)** HomeView window.__celebration 调试入口未清理
- **R2-P02 (低)** crossTabSync BroadcastChannel 从未关闭
- **R2-P03 (低)** memoStorage setInterval(30s) 模块级未清理
- **R2-P04 (低)** hybrid.ts ~500 行离线架构死代码
- **R2-P05 (低)** markdownExport.ts innerHTML 可改正则
- **R2-P06 (低)** MemoMention.ts 已禁用但文件仍存在

### 通过项 (42 项)
- 第一轮 37 项全部修复确认
- 新增 5 项通过：v-html 全部消毒 / 无 eval/Function / 定时器全部清理 / IO 全部 disconnect / .env 在 .gitignore

### 报告位置
`public/test-reports/v1.3.0-security-perf-audit-r2.html`

## 第二轮审计修复执行

用户批准 P1+P2+P3 全部修复计划，执行结果：

### P1 修复（安全）
- **R2-S01**：`supabaseClient.ts` 移除 anon key 硬编码 fallback，构建时强制要求环境变量
- **R2-S06**：`supabase.ts` 三处 Storage API 错误消息添加 `.slice(0,200).replace(/[<>"']/g,'')` 脱敏

### P2 修复（低风险安全 + 性能）
- **R2-S03**：`RichTextEditor.vue` 文件下载链接添加 `rel="noopener noreferrer"`
- **R2-S04**：`hybrid.ts` 注释从 `x-user-id header` 更新为 `JWT Bearer token (auth.uid())`
- **R2-S05**：`SlashCommand.ts` innerHTML 模板替换为 DOM API createElement + textContent
- **R2-P01**：`HomeView.vue` `window.__celebration` 限制为 `import.meta.env.DEV` + onUnmounted 清理

### P3 修复（技术债清理）
- **R2-P02**：`crossTabSync.ts` 添加 `closeCrossTabSync()` 导出函数，`App.vue` handleLogout 中调用
- **R2-P03**：`memoStorage.ts` setInterval ID 保存到 `retryIntervalId`，添加 `cleanupMemoStorage()` 导出，`App.vue` handleLogout 中调用
- **R2-P05**：`markdownExport.ts` `htmlToPlainText` 从 `div.innerHTML` 改为 DOMParser
- **R2-P06**：删除孤儿文件 `MemoMention.ts`（RichTextEditor.vue 中已注释禁用）

### 构建验证
`npx vite build` 通过，exit code 0，18.10s，无报错

## 第三轮安全与性能审计

三并行 Agent + 手动验证，覆盖 ~120 个源文件。

### 结果：0 严重 / 0 高 / 3 中 / 9 低 / 52 已通过

### 新发现问题 (12 项)
- **R3-P02 (中高)** handleLogout 未清理 Pinia store 数据，跨用户数据残留风险
- **R3-P01 (中)** memoStorage online/visibilitychange 事件监听器泄漏，重新登录重复添加
- **R3-P03 (中)** Supabase 查询未用 select= 限制字段，memos 列表返回完整 content
- **R3-S01 (低)** supabaseClient localStorage key 硬编码项目 ID
- **R3-P04 (低)** closeCrossTabSync 登出后破坏入站跨标签页同步
- **R3-P05 (低)** useTheme mediaQuery 监听器未清理
- **R3-P06 (低)** 多个短时 setTimeout 未清理
- **R3-P07 (低)** onTaskDoneCallback 未在 logout 清除
- **R3-P08 (低)** memoStore.reorderMemo 逐条 upsert (N+1)
- **R3-P09 (低)** 查询无 limit 参数
- **R3-P10 (低)** vite.config @tiptap/extension-mention 死代码
- **R3-S02 (低)** hybrid.ts 500 行死代码（保留备查）

### 报告位置
`public/test-reports/v1.3.0-security-perf-audit-r3.html`

## 第三轮审计修复执行

用户批准 P1+P2+P3 全部修复计划，执行结果：

### P1 修复（中风险）
- **R3-P02+R3-P04**：`App.vue` handleLogout 改用 `window.location.href = '/login'` 强制页面重载，自然清空所有 Pinia store 数据 + 重新注册跨标签页同步监听器。移除 `closeCrossTabSync()` 调用和导入（页面重载后自动重建）。`LoginView.vue` 所有 store load 改为 `load(true)` 强制重载。
- **R3-P01**：`memoStorage.ts` 提取匿名事件监听器为具名函数（`onOnline` / `onVisibilityChange`），`cleanupMemoStorage()` 中添加 `removeEventListener` 调用。

### P2 修复（低风险）
- **R3-P05**：`useTheme.ts` 添加 `onUnmounted` 钩子清理 `mediaQuery.removeEventListener('change', onMediaChange)`
- **R3-S01**：`supabaseClient.ts` `getCurrentUserIdSync()` 从 `SUPABASE_URL` 动态推导 projectRef 替代硬编码项目 ID
- **R3-P07**：`task.ts` 添加 `clearOnTaskDone()` 导出函数清除回调引用
- **R3-P09**：`supabase.ts` 4 个查询函数添加 `limit=` 参数（tasks 1000 / todos 500 / memos 500 / weeklyReports 200）
- **R3-P10**：`vite.config.ts` 移除 `@tiptap/extension-mention` 和 `@tiptap/suggestion` manualChunks 死代码

### 未修复（暂缓）
- **R3-P03**：select= 限制字段 — 需改备忘录加载架构（仅元数据列表 + 按需加载内容），作为后续重构
- **R3-P06**：短时 setTimeout 清理 — 风险极低，暂缓
- **R3-P08**：reorderMemo N+1 — 需 PostgREST 批量 PATCH 或 RPC，暂缓
- **R3-S02**：hybrid.ts 死代码 — 保留备查

### 构建验证
`npx vite build` 通过，exit code 0，21.82s，2557 模块，无报错

## 第四轮安全与性能审计

三并行 Explore Agent + 手动逐文件验证，覆盖 ~120 源文件。

### 结果：0 严重 / 0 高 / 2 中 / 8 低 / 55 已通过

### 新发现问题 (10 项)
- **R4-P01 (中)** handleLogout 未调用 flushGrowthToCloud，2s 防抖窗口内 Growth 数据丢失
- **R4-S01 (中)** console_sync_fix.js 硬编码用户 UUID + Supabase 凭据，未在 .gitignore 中
- **R4-P02 (低)** task.ts 模块级 visibilitychange 匿名函数无法移除
- **R4-P03 (低)** ToggleExtension addEventListener 匿名函数（editor.destroy 间接清理）
- **R4-P04 (低)** auth store onAuthStateChange 订阅未显式取消
- **R4-P05 (低)** MemoView deep watch 对大 content 性能开销
- **R4-S02 (低)** DOMPurify ADD_ATTR: ['*'] 过于宽松（MermaidNodeView + RichTextEditor）
- **R4-S03 (低)** Mermaid DOMPurify 允许 style 标签（CSS 注入风险，功能需求）
- **R4-S04 (低)** markdownExport.ts toggleToMarkdown 未转义 summary
- **R4-S05 (低)** CSP wasm-unsafe-eval 可能不需要

### 报告位置
`public/test-reports/v1.3.0-security-perf-audit-r4.html`

## 第四轮安全与性能审计

三并行 Explore Agent + 手动代码验证，覆盖 ~120 个源文件。

### 结果：0 严重 / 0 高 / 1 中 / 4 低 / 55 已通过

### 新发现问题 (5 项)
- **R4-P01 (中)** handleLogout 未调用 `flushGrowthToCloud()` — 养成数据（XP/成就/状态）使用 2s 防抖上传，登出时可能丢失
- **R4-P02 (低)** `task.ts` 模块级 `visibilitychange` 监听器使用匿名函数，无法 `removeEventListener`
- **R4-P03 (低)** `ToggleExtension.ts` summary 元素的 blur/keydown 监听器使用匿名函数
- **R4-P04 (低)** `auth.ts` `unsubAuth` 变量赋值但从未调用（死代码）
- **R4-P05 (低)** `MemoView.vue` 对 selected memo 使用 `watch(..., { deep: true })`，大内容（含 base64 图片）时性能开销

### 四轮趋势
| 轮次 | 严重 | 高 | 中 | 低 | 通过 |
|------|------|------|------|------|------|
| R1   | 3    | 8    | 15   | 11   | 18   |
| R2   | 0    | 0    | 2    | 9    | 42   |
| R3   | 0    | 0    | 3    | 9    | 52   |
| R4   | 0    | 0    | 1    | 4    | 55   |

### 修复建议
- P1: R4-P01（handleLogout 添加 flushGrowthToCloud 调用）
- P2: R4-P02 + R4-P04（具名函数 + 调用 unsubAuth）
- P3: R4-P03 + R4-P05（ToggleExtension 清理 + deep watch 优化）

### 前轮暂缓项状态
- R3-P03 select= 限制字段 — 仍暂缓（需改备忘录加载架构）
- R3-P06 短时 setTimeout 清理 — 仍暂缓（风险极低）
- R3-P08 reorderMemo N+1 — 仍暂缓（需 PostgREST 批量）
- R3-S02 hybrid.ts 死代码 — 保留备查

### 报告位置
`public/test-reports/v1.3.0-security-perf-audit-r4.html`

## 第四轮审计修复执行

用户批准 P1+P2+P3+XSS 全部修复计划，执行结果：

### P1 修复（中风险）
- **R4-P01**：`App.vue` handleLogout 中 flushTaskWrites 之后添加 `await flushGrowthToCloud()`，防止养成数据（XP/成就/状态）因 2s 防抖未上传而丢失

### P2 修复（低风险）
- **R4-P02**：`task.ts` 模块级 visibilitychange 监听器提取为具名函数 `onTaskVisibilityChange`，导出 `cleanupTaskListeners()` 函数，App.vue handleLogout 中调用
- **R4-P04**：`auth.ts` 添加 `cleanup()` 方法调用 `unsubAuth()` 取消 Supabase Auth 订阅，App.vue handleLogout 中调用

### P3 修复（技术债）
- **R4-P03**：`ToggleExtension.ts` summary 元素的 blur/keydown 监听器提取为具名函数 `onSummaryBlur`/`onSummaryKeydown`，NodeView 返回 `destroy()` 清理
- **R4-P05**：`MemoView.vue` 移除 `watch deep: true`；`addTag` 中 `editTags.value.push(val)` 改为 `editTags.value = [...editTags.value, val]` 重新赋值，浅监听即可检测变更

### XSS 安全修复
- **R4-S01**：删除 `console_sync_fix.js`（含硬编码用户 UUID + Supabase anon key 的一次性调试脚本）
- **R4-S02**：`RichTextEditor.vue` 移除 `ADD_ATTR: ['*']`（DOMPurify 默认白名单已覆盖标准 HTML 属性 + data-*）；`MermaidNodeView.vue` 替换为 SVG 属性显式白名单
- **R4-S03**：Mermaid DOMPurify 保留 `style` 标签（功能需求），`FORBID_TAGS: ['script']` + `FORBID_ATTR: ['on*']` 已阻止 XSS，CSS 注入仅限视觉干扰
- **R4-S04**：`markdownExport.ts` toggleToMarkdown 中 summary 文本 HTML 转义（`&` `<` `>` `"`）
- **R4-S05**：CSP `wasm-unsafe-eval` 保留（Mermaid 依赖 WebAssembly），添加注释说明

### 构建验证
`npx vite build` 通过，exit code 0，21.30s，2557 模块，无报错

## 第五轮安全与性能审计

3 并行 Explore Agent + 手动逐文件验证，覆盖 ~120 源文件。

### 结果：0 严重 / 0 高 / 0 中 / 6 低 / 65 已通过

### R4 修复验证：10/10 全部通过

### 新发现问题 (6 项，全部低风险)
- **R5-S01** MindmapNodeView SVG 输出未经 DOMPurify 消毒（缓解：HTML 标签剥离 + 可信库 + CSP）
- **R5-P01** onCrossTabSync 回调返回值未捕获（监听器未显式取消，页面重载自然清理）
- **R5-P02** onTaskDoneCallback 异步回调未 catch（潜在未处理 rejection）
- **R5-P03** loadPromise 失败后不重置（后续 load 返回 rejected promise）
- **R5-P04** 无全局错误处理器（unhandledrejection / error）
- **R5-S02** 图片拖放接受 SVG（Canvas 压缩可能跳过小文件）

### 五轮趋势
| 轮次 | 严重 | 高 | 中 | 低 | 通过 | 累计修复 |
|------|------|------|------|------|------|---------|
| R1   | 3    | 8    | 15   | 11   | 18   | —       |
| R2   | 0    | 0    | 2    | 9    | 42   | 37      |
| R3   | 0    | 0    | 3    | 9    | 52   | 45      |
| R4   | 0    | 0    | 2    | 8    | 55   | 45      |
| R5   | 0    | 0    | 0    | 6    | 65   | 55      |

### 报告位置
`public/test-reports/v1.3.0-security-perf-audit-r5.html`

## 第五轮审计修复执行

用户批准全部 6 项修复计划，执行结果：

### 修复清单
- **R5-S01**：`MindmapNodeView.vue` 添加 `sanitizeSvg()` 函数，renderInline + renderPreview 后清理 `<script>` 标签和 `on*` 事件属性（不破坏 markmap 内部 DOM 引用）
- **R5-P01**：`App.vue` 捕获 `onCrossTabSync` 返回的取消订阅函数 `unsubCrossTab`，`onUnmounted` 中调用清理
- **R5-P02**：`task.ts` onTaskDoneCallback 调用包裹 `Promise.resolve().catch()`，防止 unhandled rejection
- **R5-P03**：`task.ts` loadPromise 添加 `.catch()` 重置 `loadPromise = null`，允许后续 load 重试
- **R5-P04**：`main.ts` 添加 `window.addEventListener('unhandledrejection')` 和 `window.addEventListener('error')` 全局错误处理器
- **R5-S02**：`RichTextEditor.vue` 图片拖放 + 文件选择排除 `image/svg+xml`

### 构建验证
`npx vite build` 通过，exit code 0，21.88s，2557 模块，无报错

### 五轮累计
- 修复项：3+8+15+11 (R1) + 2+9 (R2) + 3+9 (R3) + 2+8 (R4) + 6 (R5) = 61 项已修复
- 前三轮暂缓 4 项保留：R3-P03 select=、R3-P06 setTimeout、R3-P08 N+1、R3-S02 死代码
