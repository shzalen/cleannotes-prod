# 2026-06-17 工作日志

## 三平台响应式适配（在 cleannotes-all-in-one 副本上完成）
- 副本路径：`D:\WorkBuddySpace\ZURU\2026-06-10-10-28-51\cleannotes-all-in-one\`
- 副本大小：4.6M（已排除 node_modules / dist / dist-app / src-tauri/target）
- **断点定义**：< 768 (mobile) | 768-1023 (tablet) | >= 1024 (desktop)
- **新增文件（5 个）**：
  - `src/composables/useMediaQuery.ts` — 响应式断点 composable（isMobile/isTablet/isDesktop + platform）
  - `src/components/MobileTabBar.vue` — 手机端底部 TabBar（5 入口：首页/任务/待办/备忘/AI）
  - `src/components/MobileHeader.vue` — 手机端顶部条（菜单/标题/头像）
  - `src/components/MobileDrawer.vue` — 手机端抽屉式侧栏（含所有 7 个导航 + 退出登录）
  - `src/styles/responsive.css` — 全局基础规则（tap-highlight/最小 44px/hover:none 重置/safe-area/dvh 高度/平台 hide-show 工具类）
- **修改文件**：
  - `index.html`：viewport 增强（maximum-scale=1.0 / viewport-fit=cover / theme-color / apple-mobile-web-app-capable）
  - `src/main.ts`：导入 responsive.css
  - `src/App.vue`：shell 路由形态分支（桌面侧栏/平板 64px 折叠/手机顶栏+底栏+抽屉）
  - `src/views/HomeView.vue` / `StatsCards.vue`：@media 断点扩展为 767/1023 双断点
  - `src/views/TasksView.vue`：双列 360+1fr → 手机单列（日历+任务列表），平板 300+1fr
  - `src/views/TodoView.vue` / `MemoView.vue` / `SpiritView.vue` / `SettingsView.vue`：page-header / 内边距 / 网格响应式
  - `src/components/MemoEditModal.vue` / `TaskEditModal.vue`：手机端 fullscreen（100% × 100dvh，圆角 0）
- **App.vue 平板态**：用 :deep() 选择器把侧栏收紧到 64px，隐藏 brand-text/user-info/nav-label/状态文字
- **手机态 padding-bottom：64px** 留出 TabBar 空间
- **构建结果**：vite build 成功（6.55s，零错误）；vue-tsc 有 2 个 pre-existing 错误（RichTextEditor setContent + task.ts toLocalDate），与本次改动无关

## Git 仓库撤销（用户改为本地副本方案）
- 已撤销：原计划 push 到 GitHub 的方案
- 原因：用户选择本地副本策略，不引入 Git 远程依赖
- 已保留：cleannotes 目录的 .git 仍存在（init 后的产物），不影响原项目使用

## Git 仓库初始化（v1.0-stable 封版 — 撤销）
- `cleannotes` 此前从未初始化 Git，本次从零开荒
- `.gitignore` 覆盖：node_modules / dist / dist-app / installer/build / *.exe / .env / .vscode / .idea / nul 等
- 首次 commit `d4aae18` 包含 99 个文件（27,989 行），作为 v1.0-stable 封版基线
- Tag `v1.0-stable` 已打，绑定到 main 分支顶端
- 已创建 `feat/mobile-responsive` 分支，待做平板/手机适配
- **未完成**：GitHub remote 关联 + push（用户改为本地副本方案，未推送）
- 验证：`git log --all --decorate` 显示 commit 同时挂在 main / feat/mobile-responsive / v1.0-stable

## 新建任务草稿缓存精简
- TaskEditModal.vue 草稿系统从缓存全部7个字段精简为仅缓存 title + description
- 优先级、日期、时间每次打开新建弹窗时重置为默认值（medium/当天/当前时刻）
- saveDraft() hasContent 判断改为仅检查标题和描述
- loadDraft() 只恢复 title/description，不再覆盖 openNew() 设好的默认值
- 构建：零错误